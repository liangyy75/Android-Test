apply from: rootProject.file('gradles/global/module.gradle')

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
    api 'androidx.appcompat:appcompat:1.1.0'

    api project(":context-ktx")
}
